<h1>Todos os livros</h1>

<ul>
  <% @books.each do |book| %>
  <li>
    <%= book.title %> de <%= book.author %>
    <%= link_to 'Editar', edit_book_path(book) %>
    <%= link_to 'Remover', book_path(book), method: :delete, data: { confirm: "Tem certeza?" } %>
  </li>
  <% end %>
</ul>
